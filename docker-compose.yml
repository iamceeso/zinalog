services:
  zinalog:
    image: ghcr.io/iamceeso/zinalog:latest
    restart: unless-stopped
    ports:
      - "4000:4000"
    environment:
      PORT: "4000"
      HOSTNAME: "0.0.0.0"
      DATABASE_PATH: /data/logs.db
      TRUST_PROXY: "false"
    volumes:
      - ./data:/data